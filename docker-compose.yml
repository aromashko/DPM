version: "3.8"

services:
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: docx_worker_rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-guest}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  worker:
    build: .
    container_name: docx_worker
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      RABBITMQ_HOST: ${RABBITMQ_HOST:-rabbitmq}
      RABBITMQ_PORT: ${RABBITMQ_PORT:-5672}
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASS: ${RABBITMQ_PASS:-guest}
      INPUT_QUEUE: ${INPUT_QUEUE:-docx_tasks}
      OUTPUT_QUEUE: ${OUTPUT_QUEUE:-docx_results}
      ERROR_QUEUE: ${ERROR_QUEUE:-docx_errors}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LIBREOFFICE_PATH: ${LIBREOFFICE_PATH:-libreoffice}
    restart: unless-stopped

volumes:
  rabbitmq_data:
